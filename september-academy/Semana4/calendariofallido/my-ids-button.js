import { LitElement, css, html } from "lit";
import { type } from "os";
import { title } from "process";

export class MyIdsForm extends LitElement {
    static properties = {
        title: {
            type: String,

        },
        buttonText: {

        }
    };

static css
}